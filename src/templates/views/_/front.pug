section(class="canvas front" id="front")

    div(class="content")

        h2 Platframe

        .call

            p Structured, scalable and modular frontend platform.

        .action

            a(href="#gist") Get the gist
            a(href="/docs/") Read the docs

    //-
        receives the version from package.json, should thus maintain "artificial"
        release equality with platframe (core) - in this repo's package config

    - bannerText = "v" + process.env.npm_package_version
    
    +nav-1("next", "nav-1-stroke")
    +banner-1("//github.com/platframe/platframe", bannerText)
